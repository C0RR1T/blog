---
import { Image } from "astro:assets"


const { src, alt, desc, width, height} = Astro.props
---

<figure class="flex items-center flex-col justify-center text-center">
  <Image src={src} alt={alt} width={width} height={height} />
  <figcaption>{desc}</figcaption>
</figure>
